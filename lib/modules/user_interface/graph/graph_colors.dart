import 'dart:ui';

class GraphColors {
  final Color graphBackgroundColor;
  final Color graphLineColor;
  final Color graphIncrementLineColor;

  GraphColors({
    required this.graphBackgroundColor,
    required this.graphLineColor,
    required this.graphIncrementLineColor,
  });
}